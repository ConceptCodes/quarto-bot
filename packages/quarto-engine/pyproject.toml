[project]
name = "quarto-engine"
version = "0.1.0"
description = "Core game logic for Quarto"
requires-python = ">=3.10"
dependencies = [
    "pydantic",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["quarto_engine"]
